{
  "mcp_tools": [],

  "api_tools": [
    {
      "name": "weather_forecast",
      "description": "Get weather forecast for any location using RapidAPI Weather API. Returns 3-hour interval forecasts with temperature, humidity, wind speed, and weather conditions.",
      "endpoint": "https://weather-api167.p.rapidapi.com/api/weather/forecast",
      "method": "GET",
      "headers": {
        "x-rapidapi-key": "93b538cb11msh05c0ef72610bd44p1f13ddjsn9155b4fae6ca",
        "x-rapidapi-host": "weather-api167.p.rapidapi.com",
        "Accept": "application/json"
      },
      "params": {
        "place": {
          "type": "string",
          "required": true,
          "description": "Location name with country code (e.g., 'London,GB', 'Beijing,CN', 'New York,US')"
        },
        "cnt": {
          "type": "integer",
          "required": false,
          "default": 10,
          "description": "Number of forecasts to return (1-40, default 10)"
        },
        "units": {
          "type": "string",
          "required": false,
          "default": "metric",
          "description": "Units: 'metric' (Celsius, m/s) or 'standard' (Kelvin, m/s)"
        },
        "type": {
          "type": "string",
          "required": false,
          "default": "three_hour",
          "description": "Forecast interval: 'three_hour' or 'daily'"
        },
        "mode": {
          "type": "string",
          "required": false,
          "default": "json",
          "description": "Response format: 'json' or 'xml'"
        },
        "lang": {
          "type": "string",
          "required": false,
          "default": "en",
          "description": "Language code (e.g., 'en', 'zh', 'es', 'fr')"
        }
      }
    },
    {
      "name": "weather_forecast_free",
      "description": "Get weather forecast for any location worldwide using Open-Meteo API (free, no authentication required). Returns daily forecast including temperature, wind speed, and precipitation data. Use this as fallback when RapidAPI is unavailable.",
      "endpoint": "https://api.open-meteo.com/v1/forecast",
      "method": "GET",
      "headers": {
        "Accept": "application/json"
      },
      "params": {
        "latitude": {
          "type": "number",
          "required": true,
          "description": "Latitude of the location"
        },
        "longitude": {
          "type": "number",
          "required": true,
          "description": "Longitude of the location"
        },
        "daily": {
          "type": "string",
          "required": false,
          "default": "temperature_2m_max,temperature_2m_min,precipitation_sum,wind_speed_10m_max",
          "description": "Comma-separated list of daily weather variables (temperature_2m_max, temperature_2m_min, precipitation_sum, wind_speed_10m_max, etc.)"
        },
        "temperature_unit": {
          "type": "string",
          "required": false,
          "default": "celsius",
          "description": "Temperature unit: 'celsius', 'fahrenheit'"
        },
        "timezone": {
          "type": "string",
          "required": false,
          "default": "auto",
          "description": "Timezone for output (e.g., 'auto', 'UTC', 'Asia/Shanghai')"
        }
      }
    },
    {
      "name": "github_user_info",
      "description": "Get GitHub user information including repos, followers, and public activity. Free API, no authentication required.",
      "endpoint": "https://api.github.com/users/{username}",
      "method": "GET",
      "headers": {
        "Accept": "application/json"
      },
      "params": {
        "username": {
          "type": "string",
          "required": true,
          "description": "GitHub username (e.g., 'torvalds', 'guido')"
        }
      }
    }
  ],

  "code_tools": []
}
